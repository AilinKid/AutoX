interface:
  display_name: "AutoX Optimize SQL"
  short_description: "Run the complete audited TiDB SQL optimization flow"
  default_prompt: "Use $autox-optimize-sql to diagnose and optimize this TiDB SQL case."
