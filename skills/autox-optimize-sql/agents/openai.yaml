interface:
  display_name: "AutoX Optimize SQL"
  short_description: "Diagnose TiDB Cloud slow SQL and produce reviewed actions"
  default_prompt: "Use $autox-optimize-sql to triage slow SQL for this TiDB Cloud cluster_id, validate candidate plans when possible, and return review-only recommendations."
